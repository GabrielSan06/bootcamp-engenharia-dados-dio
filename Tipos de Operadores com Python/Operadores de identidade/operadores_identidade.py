# Declarando duas variáveis
saldo = 1000
limite = 500

# Verificando se as duas variáveis ocupam a mesma região de memória
print(saldo is limite)

# Verificando o contrário
print(saldo is not limite)